const initialState = {
    userInfo: {},
    guestInfo: {},
    roundsToWin: 0,
    customImages: "",
}

export default function reducer(state = initialState, action){
    switch(action.type){
        
        
        default:
        return state;
    }
}